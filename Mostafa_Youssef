mkdir VCS.2
git init
git remote add ----
vim Mostafa_Youssef
git add Mostafa_Youssef
git commit -m "Steps File added"
git branch -M main
git push origin main

git branch dev
git branch test
git checkout dev
touch file1 file2
git add .
git commit -m "dev branch has two files add"
git push origin dev
git push origin test

git checkout main


remove remotely:
git push origin :dev
git push origin :test

remove localy:
 git branch -D test
 git branch -D dev

